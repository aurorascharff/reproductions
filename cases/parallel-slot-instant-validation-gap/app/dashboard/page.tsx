// `children` slot — renders normally. Required so the route resolves.
export const unstable_instant = true;

export default function DashboardPage() {
  return <p>dashboard/page.tsx (children slot)</p>;
}
